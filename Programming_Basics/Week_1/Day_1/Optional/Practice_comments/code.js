// likeCount is a variable that contain number of likes
var likeCount = 0;
// the funcion of this block is to increase likeCount
function increaseLikes() {
    // this operation increase the variable likeCount by 1
    likeCount = likeCount + 1;
}
